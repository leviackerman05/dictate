name: Dictate build

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Test core and app targets
        run: swift test
      - name: Build Dictate
        run: swift build -c release --product Dictate
